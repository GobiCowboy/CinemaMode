# 205 构建和发布

## 1. 构建

```bash
# 构建命令
```

## 2. 版本号规则

写清楚版本号怎么定（示例：`主版本.次版本.修订号`，或直接用 `git tag` + 日期）。

## 3. 发布前检查

1. 运行测试
2. 运行构建
3. 检查日志正常输出
4. 检查无 console.log
5. 检查文档索引已更新
6. 检查功能索引已更新

## 4. 发布产物

| 产物 | 路径 | 用途 |
|------|------|------|
| | | |

## 5. 回滚方式

发布失败后如何回滚（示例：切回上一版本 tag → 重新部署）。
