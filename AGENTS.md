# CinemaMode Agent Notes

- Keep the app minimal; no settings, no preferences UI.
- Build with `swift build`.
- Run locally with `./script/build_and_run.sh`.
- Prepare release artifacts with `./scripts/release_arm64.sh`.
- Follow the docs in `docs/` before changing product behavior.
